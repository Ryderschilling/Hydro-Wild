{
  "name": "hydrowild-headless",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-blog-posts.mjs && node scripts/generate-product-pages.mjs",
    "dev": "vite",
    "prebuild": "node scripts/generate-blog-posts.mjs && node scripts/generate-product-pages.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.13"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
